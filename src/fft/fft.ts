// TODO: dft function
/**
 *
 * @param data
 * @returns
 */
const dft = (data: number[]): readonly [number[], number[]] => {
  let real: number[] = [];
  let im: number[] = [];

  return [real, im];
};
